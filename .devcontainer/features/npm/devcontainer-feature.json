{
    "name": "npm packages",
    "id": "npm",
    "version": "0.0.2",
    "description": "Install npm, global Node.js packages, and optionally Playwright",
    "options": {
        "packages": {
            "type": "string",
            "default": "",
            "description": "Space-separated list of npm packages to install globally"
        },
        "playwright": {
            "type": "string",
            "default": "",
            "description": "Playwright version to install with Chromium (e.g. '1.58.2'). Empty string disables installation."
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}
